{
  "name": "file-metadata-microservice",
  "version": "1.0.0",
  "description": "Microservice that allows file upload and then outputs the file size",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "LS8",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2",
    "multer": "^1.3.0",
    "pug": "^2.0.0-beta11"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^3.2.0"
  }
}
